---
name: Lint READMEs

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    name: Lint READMEs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint
        run: make markdownlint-readmes
